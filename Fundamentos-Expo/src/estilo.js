import { StyleSheet } from "react-native";

export default StyleSheet.create({
    TxtG: {
      fontSize: 26,
      textAlign: 'center',
    },
    TxtM: {
      fontSize: 22,
      textAlign: 'center',
    },
    TxtP: {
      fontSize: 18,
      textAlign: 'center',
    },
  })