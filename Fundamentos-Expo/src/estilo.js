import { StyleSheet } from "react-native";

export default StyleSheet.create({
    TxtG: {
      fontSize: 26,
      textAlign: 'center',
    },
    TxtP: {
      fontSize: 18,
      textAlign: 'center',
    },
  })